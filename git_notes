git config --global user.email trinath.ya@gmail.com
git config --global user.name Trinath


…or create a new repository on the command line
echo "# python_class" >> README.md

git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/trinath-ya/python.git
git push -u origin master


…or push an existing repository from the command line

git remote add origin https://github.com/trinath-ya/python.git
git push -u origin master
